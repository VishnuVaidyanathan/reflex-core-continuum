[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "rcc"
version = "1.0.0"
description = "Reflex Core Continuum — layered emotional architecture for intelligent systems"
authors = [{ name = "Vishnu Vaidyanathan" }]
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
